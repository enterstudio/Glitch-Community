
- defaultAvatar = "https://cdn.gomix.com/2bdfb3f8-05ef-4035-a06e-2043962a3a13%2FdefaultAvatar%402x.png"

section.features

  h2 Featured Apps
  ul
    - application = @
    - @featuredProjects().forEach (project) ->
      li
        - showProjectOverlay = -> application.showProjectOverlay project
        .featured-container(click=showProjectOverlay data-track="featured project" data-track-label=project.name)
          img.featured(width=30 height=30 src=project.img)

          p.project-name= project.name

        - # refactor me into includes/users.jadelet
        - if project.users
          .users
            - project.users.forEach (user) ->
              .user(title=user.login data-tooltip=user.login data-tooltip-left=true)
                img(width=30 height=30 src=user.avatarUrl)
        - else
          .users
            .user(title="Gomix team" data-tooltip="Gomix team" data-tooltip-left=true)
              img(width=30 height=30 src=defaultAvatar)
